#!/bin/bash

sudo cp ~/.local/share/aura/default/plymouth/* /usr/share/plymouth/themes/aura/
sudo plymouth-set-default-theme aura

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
