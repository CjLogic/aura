#!/bin/bash

aura-cmd-present "$EDITOR" || EDITOR=micro

case "$EDITOR" in
com.visualstudio.code | fresh | nano | micro | hx | helix)
  exec aura-launch-tui "$EDITOR" "$@"
  ;;
*)
  exec setsid uwsm-app -- "$EDITOR" "$@"
  ;;
esac
