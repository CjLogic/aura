#!/bin/bash

set -e

FIRST_RUN_MODE=~/.local/state/aura/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$AURA_PATH/install/first-run/battery-monitor.sh"
  bash "$AURA_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$AURA_PATH/install/first-run/firewall.sh"
  bash "$AURA_PATH/install/first-run/dns-resolver.sh"
  bash "$AURA_PATH/install/first-run/gnome-theme.sh"
  bash "$AURA_PATH/install/first-run/elephant.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$AURA_PATH/install/first-run/refresh-database.sh"
  bash "$AURA_PATH/install/first-run/welcome.sh"
  bash "$AURA_PATH/install/first-run/aura-fish-shell.sh"
fi
