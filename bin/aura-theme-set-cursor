#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
aura-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/aura/toggles/skip-cursor-theme-changes" Cursor
